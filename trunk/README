How to install:

Install in default directory (/usr/local)
	./configure
	make
	su
	(print root password in)
	make install
	
Install in prefered directory (/bla/bla)
	./configure --prefix=/blah/blah
	make
	make install
	
 by problems arti_tevs@yahoo.de